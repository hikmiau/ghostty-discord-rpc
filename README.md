# Kitty Discord RPC

A small Discord Rich Presence app for Kitty terminal on Linux.

## Features

- Shows Kitty as your Discord activity
- Shows the current project/folder
- Uses configurable Discord assets
- Pauses when Neovim/Vim RPC is running
- Can run automatically with a systemd user service

## Setup

Install dependencies:

```bash
npm install
